/* tabs.js — Tabbed case study panel (Phase 1 implementation)
   Consumed by render.js renderTabs() — see render.js for the
   full implementation. This file reserved for standalone init if needed.
*/

export function initTabs(container) {
  // Phase 1: implemented via renderTabs() in render.js
}
