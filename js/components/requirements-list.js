/* requirements-list.js — 11-requirement expandable list (Phase 1)
   Consumed by render.js renderExpandableList() — see render.js.
*/

export function initRequirementsList(container) {
  // Phase 1: implemented via renderExpandableList() in render.js
}
