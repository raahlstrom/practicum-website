/* state-map.js — SVG U.S. state map with opt-in status (Phase 1)
   Renders color-coded state outlines from content/state-map-data.js
   and shows tooltip on hover/tap.
*/

export function initStateMap(container, stateData) {
  // Phase 1: implement SVG state map + tooltip
}
