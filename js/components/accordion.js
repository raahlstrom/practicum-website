/* accordion.js — Three-panel accordion (Phase 1 implementation)
   Consumed by render.js renderAccordion() — see render.js for the
   full implementation. This file reserved for standalone init if needed.
*/

export function initAccordion(container) {
  // Phase 1: implemented via renderAccordion() in render.js
}
