/* flow-diagram.js — 5-step SVG fund flow with scroll animation (Phase 1)
   Renders the TAXPAYER → SGO → STUDENT flow diagram and triggers
   IntersectionObserver-based build animation on scroll.
*/

export function initFlowDiagram(container, steps) {
  // Phase 1: implement SVG render + IntersectionObserver animation
}
